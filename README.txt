                      === ANTI-ANTI-ALIASER ===

Small script that resizes small images to a large-enough resolution
without using anti-aliasing, so they can be sent into instant
messaging apps without looking really small.

                            == Running ==

meson setup build
meson compile -C build
./build/aaa
